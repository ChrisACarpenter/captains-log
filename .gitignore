.DS_Store
*.swp
*.swo

# Node / web frontend (when we get there)
node_modules/
dist/
build/
.vite/
.next/

# Playwright test-runner artifacts (Playwright drops .last-run.json and any
# retained screenshots/traces here between runs — not source of truth).
test-results/

# Rust / Tauri
target/
**/target/
Cargo.lock.user

# Local environment / IDE
.env
.env.local
.idea/
.vscode/settings.json

# Local journal data, in case the user ever points the journal root inside the repo for testing
test-journals/
.test-data/

# Local-only working docs. ROADMAP.md is Chris's phase-by-phase working
# record (a lot of it reads as personal notes-to-self); DEVELOPMENT-JOURNAL.md
# is the raw session-by-session log. Both live on disk for the author's
# own reference but aren't meant to travel with the public repo.
/ROADMAP.md
/DEVELOPMENT-JOURNAL.md
