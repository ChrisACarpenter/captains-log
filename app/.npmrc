# Use the public npm registry for this homebrew project.
# The user-level ~/.npmrc defaults to Prodigy's internal CodeArtifact
# registry, which requires auth and isn't needed for Captain's Log
# (all our deps — Tauri, Svelte, Lucide — are public packages).
registry=https://registry.npmjs.org/
